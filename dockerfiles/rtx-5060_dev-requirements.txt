###############################################################################
# BASE IMAGE: nvcr.io/nvidia/pytorch:26.03-py3
#
# NOTE:
# PyTorch stack is intentionally EXCLUDED from this file.
# torch and torchvision come pre-installed as NVIDIA-optimized builds
# in the NGC PyTorch base image. Reinstalling them from the standard
# PyPI index would silently replace those builds with generic wheels
# and lose Blackwell-specific optimizations.
#
# torchaudio is NOT included in any NGC PyTorch container; it is
# compiled from source in Dockerfile Step 10 with sm_120 CUDA flags.
# Do NOT add it here, see the inline note in the DEEP LEARNING section.
#
# llama-cpp-python is also excluded, it is compiled from source
# directly in the Dockerfile with sm_120 / CUBLAS flags.
###############################################################################

###############################################################################
# CORE AI & DATA SCIENCE
###############################################################################
pandas
scikit-learn
matplotlib
seaborn
faiss-cpu                  # Vector search (CPU build)

###############################################################################
# DEEP LEARNING & LLMs
# torch / torchvision -> PRE-INSTALLED in NGC base, do not add.
# NOTE: torchaudio is NOT included in any NGC PyTorch container.
#       It is compiled from source in the Dockerfile (Step 10) against
#       the NGC torch build with TORCH_CUDA_ARCH_LIST=12.0 (Blackwell).
#       Do NOT add it here, pip would pull a community wheel with a
#       hard torch== pin that would replace the NVIDIA-optimized build.
###############################################################################
accelerate                 # Hugging Face distributed training/inference
sentence-transformers      # Embeddings (uses pre-installed torch)
transformers               # Explicit pin ensures consistent HF ecosystem

###############################################################################
# COMPUTER VISION & DOCUMENT PROCESSING
###############################################################################
opencv-python
pdf2image
pypdf                      # Successor to the deprecated PyPDF2 package
pytesseract
python-docx
openpyxl
beautifulsoup4

###############################################################################
# BACKEND & API STACK
###############################################################################
fastapi
uvicorn[standard]          # [standard] pulls in uvloop + httptools; covers the
                           # separate httptools entry from v1.0.0
pydantic-settings
python-multipart
websockets
watchfiles
python-dotenv

###############################################################################
# UTILITIES & SYSTEM
###############################################################################
cmake                      # Required for llama-cpp-python source build
pydot                      # Model visualization (requires graphviz system pkg)
markdown
hf-xet                     # Fast large-file transfer from Hugging Face Hub
ipywidgets                 # Interactive Jupyter widgets
# tomli removed, Python 3.12 ships tomllib in the standard library
# httptools removed, covered by uvicorn[standard]